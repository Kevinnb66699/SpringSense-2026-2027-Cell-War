# AGENTS.md

本项目的 AI 开发规则（对所有 AI 编码工具通用，不限于 Claude）：

1. 先读根目录 `CLAUDE.md`（十条铁律精简版）；
2. 改代码前完整阅读 `docs/架构说明书.md`（模块地图、常见任务菜谱、禁止清单）；
3. 每次改动跑 `game/tests/headless_test.gd` 无头测试，全绿才能提交。

请严格遵循既有架构（问答桥 + 纯逻辑核心 + 确定性），不要按自己的偏好另起炉灶。
